package examples

fun demonstrateHomeworkSolutions() {
    println("\n=== Домашние задания - постановка задач ===\n")
    
    homework1()
    homework2()
    homework3()
    practiceExercises()
}

// ДЗ №1: Обобщенная функция transformCollection
fun homework1() {
    println("ДЗ №1: Обобщенная функция transformCollection")
    println("=" * 50)
    
    println("""
    📝 Задание:
    Напишите обобщенную функцию transformCollection, которая:
    - Принимает коллекцию элементов типа T
    - Принимает функцию преобразования из T в R
    - Возвращает список элементов типа R
    
    💡 Подсказки:
    - Используйте generics (<T, R>)
    - Функция-параметр должна иметь тип (T) -> R
    - Результат должен быть List<R>
    
    🎯 Примеры использования:
    val numbers = listOf(1, 2, 3, 4, 5)
    val squared = transformCollection(numbers) { it * it }
    // squared должен быть [1, 4, 9, 16, 25]
    
    val words = listOf("Hello", "Kotlin", "QA")
    val lengths = transformCollection(words) { it.length }
    // lengths должен быть [5, 6, 2]
    
    ⭐ Дополнительное задание:
    Создайте функцию-расширение transformTo для Collection<T>
    """.trimIndent())
    
    // Показываем только сигнатуру функции
    println("\n📌 Сигнатура функции:")
    println("fun <T, R> transformCollection(collection: Collection<T>, transform: (T) -> R): List<R>")
    
    println("\n✅ Проверьте свое решение с помощью тестовых данных выше!")
}

// ДЗ №2: Группировка и фильтрация
fun homework2() {
    println("\n\nДЗ №2: Группировка и фильтрация")
    println("=" * 50)
    
    println("""
    📝 Задание 1: Группировка людей по возрасту
    
    Дан список людей:
    data class Person(val name: String, val age: Int)
    
    val people = listOf(
        Person("Петя", 25),
        Person("Вася", 30),
        Person("Даша", 25),
        Person("Женя", 30),
        Person("Алексей", 20)
    )
    
    Необходимо:
    1. Сгруппировать людей по возрасту
    2. Вывести для каждого возраста список имен
    3. Подсчитать количество людей каждого возраста
    
    💡 Подсказки:
    - Используйте функцию groupBy { }
    - Для подсчета используйте mapValues { it.value.size }
    - Результат группировки - Map<Int, List<Person>>
    
    📝 Задание 2: Фильтрация и группировка слов
    
    Дан список слов:
    val words = listOf("Apple", "Banana", "Apricot", "Cherry", "Avocado", "Almond")
    
    Необходимо:
    1. Отфильтровать слова, начинающиеся на 'A' (без учета регистра)
    2. Сгруппировать отфильтрованные слова по их длине
    3. Вывести результат
    
    💡 Подсказки:
    - Сначала filter { }, потом groupBy { }
    - Для проверки первой буквы: startsWith("A", ignoreCase = true)
    - Длина строки: it.length
    
    ⭐ Дополнительное задание:
    Используйте groupingBy для подсчета слов по первой букве
    """.trimIndent())
    
    println("\n✅ Проверьте свои решения с данными выше!")
}

// ДЗ №3: DSL для HTTP-запросов
fun homework3() {
    println("\n\nДЗ №3: DSL для HTTP-запросов")
    println("=" * 50)
    
    println("""
    📝 Задание: Создать DSL для формирования HTTP-запросов
    
    Необходимо реализовать DSL, который позволит писать код в таком стиле:
    
    val request = post {
        url("https://api.example.com/users")
        header {
            contentType("application/json")
            authorization("Bearer token123")
        }
        body(${"\"\"\""}{"name": "John", "age": 30}${"\"\"\""})
    }
    
    Требования:
    1. Поддержка методов: GET, POST, PUT, DELETE
    2. Возможность задать URL
    3. Возможность добавить заголовки
    4. Возможность добавить тело запроса (для POST/PUT)
    
    💡 Подсказки по структуре:
    
    1. Создайте data class HttpRequest:
       - method: String
       - url: String
       - headers: Map<String, String>
       - body: String?
    
    2. Создайте builder классы:
       - HttpRequestBuilder(method: String)
       - HeaderBuilder
    
    3. В HttpRequestBuilder добавьте методы:
       - fun url(value: String)
       - fun body(value: String)
       - fun header(config: HeaderBuilder.() -> Unit)
    
    4. Создайте DSL функции:
       - fun post(config: HttpRequestBuilder.() -> Unit): HttpRequest
       - fun get(config: HttpRequestBuilder.() -> Unit): HttpRequest
       - и т.д.
    
    🎯 Пример использования вашего DSL:
    
    // GET запрос
    val getUser = get {
        url("https://api.example.com/users/123")
        header {
            authorization("Bearer token")
        }
    }
    
    // POST запрос
    val createUser = post {
        url("https://api.example.com/users")
        header {
            contentType("application/json")
            authorization("Bearer token")
        }
        body(${"\"\"\""}{"name": "Alice", "email": "alice@example.com"}${"\"\"\""})
    }
    
    ⭐ Дополнительные задания:
    1. Добавьте поддержку query параметров
    2. Добавьте валидацию (например, body только для POST/PUT)
    3. Создайте функции для часто используемых заголовков
    """.trimIndent())
    
    println("\n✅ Протестируйте ваш DSL с примерами выше!")
}

// Практические упражнения
fun practiceExercises() {
    println("\n\n=== Практические упражнения для самостоятельной работы ===")
    println("=" * 50)
    
    println("""
    📝 Упражнение 1: Основы Kotlin
    
    Задания:
    1. Создайте переменную name типа String со значением вашего имени
    2. Создайте изменяемую переменную age с вашим возрастом
    3. Создайте nullable переменную email типа String?
    4. Выведите все переменные, используя string interpolation
    5. Для email используйте Elvis оператор для значения по умолчанию
    
    💡 Подсказка: val vs var, тип?, Elvis оператор (?:)
    
    
    📝 Упражнение 2: Функции
    
    Напишите функцию checkTestResult, которая:
    - Принимает testName: String и passed: Boolean
    - Возвращает строку "PASS" или "FAIL"
    - Используйте краткую запись функции
    
    💡 Подсказка: fun name(params) = if (condition) ... else ...
    
    
    📝 Упражнение 3: Работа с коллекциями
    
    Дан список тестов:
    val tests = listOf("login", "api", "logout", "ui", "performance")
    
    Задания:
    1. Отфильтруйте тесты длиннее 4 символов
    2. Преобразуйте все тесты в верхний регистр
    3. Найдите первый тест, содержащий букву 'o'
    4. Проверьте, все ли тесты короче 15 символов
    
    💡 Подсказки: filter, map, find, all
    
    
    📝 Упражнение 4: Generics
    
    Напишите обобщенную функцию getMiddle, которая:
    - Принимает List<T>
    - Возвращает средний элемент списка (или null для пустого)
    - Для четного количества возвращает элемент перед серединой
    
    💡 Подсказка: list.size / 2, getOrNull()
    
    
    📝 Упражнение 5: Data классы и группировка
    
    Создайте data class TestResult(val name: String, val status: String, val duration: Int)
    
    Дан список результатов:
    val results = listOf(
        TestResult("login", "PASS", 150),
        TestResult("logout", "FAIL", 80),
        TestResult("search", "PASS", 200),
        TestResult("filter", "PASS", 120),
        TestResult("checkout", "FAIL", 300)
    )
    
    Задания:
    1. Сгруппируйте тесты по статусу
    2. Найдите среднюю длительность для каждого статуса
    3. Найдите самый долгий тест
    4. Подсчитайте процент успешных тестов
    
    💡 Подсказки: groupBy, mapValues, maxByOrNull, count
    """.trimIndent())
    
    println("\n✅ Выполните все упражнения и проверьте результаты!")
}

// Вспомогательная функция для красивого вывода
operator fun String.times(n: Int): String = repeat(n)